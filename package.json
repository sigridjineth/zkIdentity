{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.5",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "chai": "^4.3.6",
    "ethereum-waffle": "^3.4.4",
    "ethers": "^5.6.4",
    "hardhat": "^2.9.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.5.0",
    "circomlib": "^2.0.3",
    "zkey-manager": "^0.1.2"
  },
  "scripts": {
    "test": "hardhat test",
    "circuits": "mkdir circuits/build && circom circuits/verify_winner.circom --r1cs --wasm --sym --c -o circuits/build"
  }
}
